{% comment %}
  Template: Financing Page with HeyLight Calculator
  This template displays financing information and calculator
{% endcomment %}

<div class="page-width">
  <header class="section-header">
    <h1 class="section-header__title">{{ page.title }}</h1>
  </header>

  {% if page.content != blank %}
    <div class="rte">
      {{ page.content }}
    </div>
  {% endif %}

  {% section 'financing-calculator' %}
  
  {% comment %} Additional financing sections can be added here {% endcomment %}
  {% section 'financing-benefits' %}
  {% section 'financing-faq' %}
</div>