{% comment %}
  Template: Why Choose Us Page
  This template displays reasons to choose Godspeed
{% endcomment %}

<div class="page-width">
  <header class="section-header">
    <h1 class="section-header__title">{{ page.title }}</h1>
  </header>

  {% if page.content != blank %}
    <div class="rte">
      {{ page.content }}
    </div>
  {% endif %}

  {% section 'why-reasons' %}
  {% section 'testimonials' %}
  {% section 'awards' %}
  {% section 'guarantees' %}
</div>